<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <link rel="stylesheet" type="text/css" href="/main.css">
  </head>
  <body>
    <% flash.keys.each do |type| %>
      <%=flash[type] %>
    <% end %>
   <%= yield %>
 </body>
</html>
