<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <title></title>
  </head>
  <body>
    <% flash.keys.each do |type| %>
      <%= flash[type] %>
    <% end %>
    <h1>welcome to the FT's rating site</h1>
    <form action='/' method='post'>
      <% @rating_range.each do |score| %>
        <%= "<input type='radio' name='rating' value=#{score} id=#{score} class='rating'>" %>
      <% end %>
      <input type='submit' value='Submit' />
    </form>
  </body>
</html>
